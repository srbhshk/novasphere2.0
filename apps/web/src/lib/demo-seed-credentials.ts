/**
 * Demo credentials for the seeded database (see `apps/web/scripts/seed-database.ts`).
 * Single place to keep sign-in UX and seed script aligned.
 */
export const DEMO_SEED_EMAILS = [
  'admin@demo.com',
  'ceo@demo.com',
  'eng@demo.com',
] as const

export const DEMO_SEED_PASSWORD = 'password'
